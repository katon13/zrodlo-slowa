<?xml version="1.0" encoding="utf-8"?>
<!--
    Manifest dołączany wyłącznie do wariantu debug. Dodaje deweloperski schemat
    dors3-dev:// dla przepływu "ten sam telefon" bez konieczności zweryfikowanego
    Android App Link podczas lokalnych testów. Nigdy nie trafia do wariantu release.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="${dors3DebugScheme}"
                    android:host="approve" />
            </intent-filter>
        </activity>
    </application>

</manifest>
