<?xml version="1.0" encoding="utf-8"?>
<!--
    Konfiguracja bezpieczeństwa sieci wyłącznie dla wariantu debug. Dopuszcza
    ruch cleartext TYLKO do adresów lokalnych/emulatora (10.0.2.2, localhost).
    Ruch globalny cleartext pozostaje zablokowany (base-config secure).

    UWAGA (dyspozycja V3, pkt 8.2): `<domain>192.168.0.0</domain>` NIE oznacza
    podsieci — network_security_config.xml nie obsługuje masek CIDR w taki
    sposób, więc taki wpis dopuszczał cleartext wyłącznie dla dokładnie tego
    jednego adresu, dając fałszywe poczucie bezpieczeństwa. Dla telefonu
    fizycznego zalecany jest tunel `adb reverse` i debug URL 127.0.0.1. Połączenie
    LAN jest dozwolone wyłącznie przez HTTPS z certyfikatem zaufanym przez telefon;
    nie dopisujemy na stałe prywatnych adresów cleartext.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
