<?xml version="1.0" encoding="utf-8"?>
<!--
    Konfiguracja bezpieczeństwa sieci wyłącznie dla wariantu debug (ETAP 2
    z dyspozycji). Dopuszcza ruch cleartext TYLKO do adresów lokalnych/
    emulatora (10.0.2.2, localhost, 127.0.0.1) — potrzebne wyłącznie do
    lokalnych testów z serwerem deweloperskim. Ruch globalny cleartext
    pozostaje zablokowany (base-config secure), tak samo jak w istniejącej
    aplikacji mobile/3dors-android.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
